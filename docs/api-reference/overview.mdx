---
title: API Overview
---

# API Overview

Document your product surface area with clear explanations and examples. Use code blocks to highlight requests and responses or embed Mintlify components for interactive demos.

```json
{
  "endpoint": "/v1/examples",
  "method": "GET",
  "description": "Retrieve a list of examples"
}
```

Add additional pages inside this folder and update the `navigation` section of `mint.json` to keep your sidebar in sync.
